parameters:
  repo_url: ''
  repo_name: ''
  target_branch: ''

steps:
- script: cd ./packages/bot && ./scripts/clone.ts --repo-url ${{ parameters.repo_url }} --repo-name ${{ parameters.repo_name }} --branch ${{ parameters.target_branch }} --dir ../..
