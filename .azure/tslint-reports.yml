parameters:
  repo_name: ''

steps:
- script: cd ./packages/bot && ts-node ./scripts/tslint-reports.ts --repo-name ${{ parameters.repo_name }} --dir ../.. && ls -al
- task: PublishBuildArtifacts@1
  inputs:
    pathtoPublish: ./sub/.results
    artifactName: .results
